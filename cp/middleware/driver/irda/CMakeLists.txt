if (CONFIG_SUPPORT_IRDA)
	list(APPEND incs irda)
	list(APPEND srcs "irda/irda_driver.c")
	if (CONFIG_CLI AND CONFIG_IRDA_TEST)
		list(APPEND srcs "irda/irda_test.c")
	endif()
	set(incs ${incs} PARENT_SCOPE)
	set(srcs ${srcs} PARENT_SCOPE)
endif()
