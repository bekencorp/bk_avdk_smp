if (CONFIG_EFUSE)
	list(APPEND incs efuse)
	list(APPEND srcs efuse/efuse_driver.c)
	if (CONFIG_CLI AND CONFIG_EFUSE_TEST)
		list(APPEND srcs efuse/efuse_test.c)
	endif()
	set(incs ${incs} PARENT_SCOPE)
	set(srcs ${srcs} PARENT_SCOPE)
else()
    list(APPEND incs efuse)
    list(APPEND srcs efuse/efuse_otp_driver.c)
endif()

set(incs ${incs} PARENT_SCOPE)
set(srcs ${srcs} PARENT_SCOPE)