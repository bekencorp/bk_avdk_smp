if (CONFIG_LIN)
	list(APPEND incs lin)
	list(APPEND srcs lin/lin_driver.c lin/lin_statis.c)
	if (CONFIG_LIN_TEST)
	list(APPEND srcs lin/lin_test.c)
	endif()
	set(incs ${incs} PARENT_SCOPE)
	set(srcs ${srcs} PARENT_SCOPE)
endif()