if (CONFIG_TRNG_SUPPORT)
	list(APPEND incs trng)
	list(APPEND srcs trng/trng_driver.c)
	if (CONFIG_CLI AND CONFIG_TRNG_TEST)
		list(APPEND srcs trng/trng_test.c)
	endif()
	set(incs ${incs} PARENT_SCOPE)
	set(srcs ${srcs} PARENT_SCOPE)
endif()