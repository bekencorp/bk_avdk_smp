if (CONFIG_PSRAM)
	list(APPEND incs psram)
	list(APPEND srcs psram/psram_driver.c)
	if (CONFIG_CLI AND CONFIG_PSRAM_TEST)
		list(APPEND srcs psram/psram_test.c)
	endif()
	set(incs ${incs} PARENT_SCOPE)
	set(srcs ${srcs} PARENT_SCOPE)
endif()